[project]
name = "fantasy-ingestion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "froilanbuendia", email = "froilangbuendia@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.74",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
poll-draft = "fantasy_ingestion.poll_draft:main"

[tool.uv.build-backend]
module-root = ""

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
